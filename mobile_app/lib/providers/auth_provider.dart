// Đã gỡ (không còn dùng): AuthProvider (ChangeNotifier) cho luồng đăng nhập JWT native, thay
// bởi bản WebView (xem lib/screens/webview_screen.dart). Xem storage_service.dart cùng gốc
// lib/ để biết lý do file này giữ rỗng thay vì bị xoá hẳn.
