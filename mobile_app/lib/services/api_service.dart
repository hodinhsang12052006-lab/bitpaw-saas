// Đã gỡ (không còn dùng): client Dio + interceptor JWT cho luồng auth native, thay bởi bản
// WebView (xem lib/screens/webview_screen.dart). Xem storage_service.dart cùng thư mục để
// biết lý do file này giữ rỗng thay vì bị xoá hẳn.
