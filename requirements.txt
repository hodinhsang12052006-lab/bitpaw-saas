# ============================================================================
# BitPaw OS — WEB APP (deploy Vercel). CHỈ liệt kê dependency THỰC SỰ được
# import bởi app.py và các module nó cần lúc chạy trên Vercel — không phải
# kiểu "pip freeze" đầy đủ mọi gói cài trên máy dev. Thư viện cho Desktop App
# (.exe offline) / test e2e nằm ở requirements_desktop.txt, KHÔNG gộp vào đây
# (đã từng làm Vercel function vượt giới hạn 500MB).
# ============================================================================

# --- Flask core ---
Flask==3.1.3
Flask-WTF==1.2.2
Werkzeug==3.1.8
Jinja2==3.1.6
MarkupSafe==3.0.3
itsdangerous==2.2.0
click==8.4.2
blinker==1.9.0

# --- JWT cho Mobile App (Giai đoạn 5 audit — /api/auth/token, login_required hybrid) ---
PyJWT==2.13.0

# --- WSGI server cho deploy Render/Procfile (app.py không cần khi chạy trên Vercel serverless,
# nhưng Procfile/render.yaml dùng gunicorn cho phần Web + giữ 3 background worker sống 24/7) ---
gunicorn==23.0.0

# --- Rate limiting (app.py: Limiter(storage_uri=RATELIMIT_STORAGE_URI or 'memory://')) ---
Flask-Limiter==4.1.1
limits==5.8.0
ordered-set==4.1.0
sortedcontainers==2.4.0
# Nếu set RATELIMIT_STORAGE_URI=redis://... trên Vercel (khuyến nghị cho production
# thật, vì 'memory://' không share giữa các serverless instance) — dùng chung gói redis==5.2.1
# khai báo ngay dưới đây (đã là dependency thật của redis_queue.py/consumer.py).

# --- Redis Streams cho check-in/check-out GPS (redis_queue.py, consumer.py — Mã 1.2 audit) ---
# Cần đặt biến môi trường REDIS_URL (vd: redis://user:pass@host:6379/0, hoặc Upstash) ở cả nơi
# chạy app.py LẪN nơi chạy consumer.py (2 process khác nhau, phải trỏ cùng 1 Redis).
redis==5.2.1

# --- MongoDB (mongo_client.py — kho dữ liệu chính) ---
pymongo==4.17.0
dnspython==2.8.0

# --- Mã hoá (app.py dùng cryptography.fernet.Fernet) ---
cryptography==49.0.0
cffi==2.0.0
pycparser==3.0

# --- HTTP client (ai_deepseek_client.py, ad_assistant.py, Square/Vapi webhook...) ---
requests==2.34.2
urllib3==2.7.0
certifi==2026.6.17
idna==3.18
charset-normalizer==3.4.7

# --- LLM client (Mã "Hợp nhất AI bằng DeepSeek" audit) ---
# SDK chính hãng OpenAI, dùng CHỈ để gọi DeepSeek qua base_url tương thích (ai_deepseek_client.py)
# — KHÔNG gọi tới OpenAI thật ở bất kỳ đâu trong hệ thống. Toàn bộ tác vụ AI text (Bot/Studio/Ads/
# Nurture) dùng DUY NHẤT DeepSeek qua client này; không còn Anthropic/Claude nào trong codebase.
openai==2.52.0
